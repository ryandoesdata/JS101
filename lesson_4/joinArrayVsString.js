/*
let arr = ['a', 'b', 'c', 'd', 'e', 'f'];
console.log(arr.join('')); // => 'a,b,c,d,e,f'
*/

let obj = { sky: 'blue', grass: 'green' };
let objArray = Object.entries(obj); // => [ [ 'sky', 'blue' ], [ 'grass', 'green' ] ]

console.log(objArray.join());