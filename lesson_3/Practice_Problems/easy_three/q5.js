/*

function isColorValid(color) {
  return (color === "blue" || color === "green");
}

*/

function isColorValid(color) {
  return (color === "blue" || color === "green");
}

console.log(isColorValid("green"));